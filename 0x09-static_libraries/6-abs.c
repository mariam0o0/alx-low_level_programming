#include"main.h"
/**
 * _abs - computes the absolute value of an integer.
 * @n: the varible to be computed
 * Return: the absolute value of a number
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	return (-n);
}
